// 79.Word Search
#include <vector>

class Solution {
public:
    const int dx[] = {-1,0,1,0};
    const int dy[] = {0,1,-1,0}; 

    bool exist(vector<vector<char>>& board, string word) {
        
    }
    
    bool find_dfs(vector<vector<char>>& board, int x, int y, string word) {
        
        if(word.empty()) {
            return true;
        }

        for(int i = 0; i < 4; i++) {
            if()
            char next 
            if(*word )
        }
        
    }
    
    
};